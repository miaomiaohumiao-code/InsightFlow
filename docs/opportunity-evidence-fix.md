# 机会证据一致性修复

- 机会总证据每类上限改为 15，双边及普通结论仍每类最多 5 条。15 容纳原先主列表与双边列表各 5 条不同引用。
- 聚合响应先做结构与引用登记检查，再按支持/反例分别合并去重。不生成引用、不截断证据、不修改输入草案。超出容量时返回带机会 ID 和字段名的错误。
- 合并后继续执行完整校验，缺少任一侧证据仍失败；后续来源与语义核验保持不变。补全反例包含关系检查。
- 不改检查点身份：保持原字段兼容，已完成任务可复用。旧失败任务继续执行时使用修复后的服务端规则；不保证消除其他类型的模型错误。
- 回归覆盖双边各 5 条引用、漏填反例、去重幂等、输入不变、伪造引用和空承接证据。52 项测试通过，无真实 AI 请求。

本次针对冗余证据列表做确定性修复，不通过静默删除不合格机会来发布不完整报告，也不增加重试次数。
