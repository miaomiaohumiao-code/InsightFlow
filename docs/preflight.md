# 上线前检查 · 2026-09-19

## 已验证

- Node.js 24.14.0：生产 build、TypeScript、ESLint 通过。
- 56 项离线测试通过，包括全部付费接口在 Vercel 公开开关关闭时返回 403、伪装 localhost 也不能绕过。
- npm audit --omit=dev：生产依赖已知漏洞报告为 0；该结论是当次数据库结果，不覆盖所有安全风险。
- 公开候选文本文件扫描：未发现当前环境密钥的原值、常见 sk/ GitHub token 格式或私钥头；不会输出匹配值。
- 浏览器构建产物扫描未发现同类秘密。
- 模拟 Vercel 的本地生产服务实测：首页 200；关闭公开 AI 时 analysis、marketing、experiment-recommendations 均返回 403，没有模型调用。
- .env.local 被忽略，.env.example 仅含空密钥字段；原文文档、个人截图与 .local 检查点已排除。
- Node.js 24.x 写入 package.json 与 lockfile；补充 GitHub CI，离线验证不需要密钥。CI 文件已准备，远端尚未执行。
- 三个 AI 接口共用公开开关；Vercel 环境不允许通过 localhost 主机名绕过。未开启时仍可使用浏览器本地输入、预览与历史结果。

## 尚未执行 / 上线前事项

- 目录不是 Git 仓库，无提交历史可扫描；初始化后的暂存文件须再检查，不能强制提交被忽略文件。
- 未创建 GitHub 仓库、未推送、未部署 Vercel；因此云端安装、构建及实际接口仍需上线验收。
- 之前在聊天中发送过的凭据应撤销并更换，新凭据只放本地环境文件及 Vercel 环境变量。
- 未选择 LICENSE；公开源码前由所有者决定授权方式。
- Online Demo 和脱敏截图仍为明确占位，不发布个人研究截图。
- 匿名 AI 调用缺少持久全站预算与身份认证；生产开放前配置外部费用限制和访问保护。
- 扫描不覆盖被排除的二进制研究资料或聊天历史，不宣称进行过完整专业安全审计。

部署参数及验收流程见 [deployment.md](deployment.md)。
