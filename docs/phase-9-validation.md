# Phase 9：UI 与交互统一

## 改动

- `src/app/ui-polish.css` 集中定义本次视觉调整：白底、低对比边框、统一按钮、输入焦点和卡片间距；减少动态装饰。
- 来源卡片分开来源信息与评论区，桌面评论区至少 300px、手机至少 260px，可纵向调整；添加来源按钮强化。
- Preview 新增数据健康摘要，区分无数据、少量样本、拆分异常和未发现明显异常；异常摘要可切换至异常记录筛选。
- 洞察区强化章节分隔；机会缺口移至品牌比较首位并占整行，仍保留证据不足的空态。
- 营销策略先展示核心消费者洞察与营销机会，其余策略字段采用更轻的分区；新增章节导航。
- 保留原有生成、引用、选择、排序和本地存储机制。

## 验证

- TypeScript 类型检查、ESLint 和 Next.js 生产构建通过。
- 使用浏览器已有样例检查桌面营销策略的两个重点区，以及机会缺口证据不足空态。
- Preview 样例正确显示 3 条评论、1 条极短文本和样本量较少提示。
- 390px 手机宽度下检查输入、预览、策略页面，均无页面横向溢出；输入框和日期未知复选框尺寸正常。
- 恢复默认浏览器尺寸；浏览器检查未发现运行错误。
- 本次未发起新的 AI 请求，不产生新的模型调用费用。

异常摘要快捷筛选已完成实现并通过类型检查；本轮浏览器预览样例没有拆分异常，未覆盖该按钮的点击验收。
